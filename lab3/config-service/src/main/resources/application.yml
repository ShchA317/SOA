server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: github.com/ShchA317/SOA/tree/main/lab3/config-service
          default-label: main
          search-paths: "{application}"
