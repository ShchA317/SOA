server:
  port: 8889

spring:
  application:
    name: org-manager

eureka:
  instance:
    hostname: localhost
    ip-address: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      fetchRegistry: true
