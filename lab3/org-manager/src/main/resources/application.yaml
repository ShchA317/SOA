spring:
  application.name: org-manager
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
      label: main
      enabled: true
